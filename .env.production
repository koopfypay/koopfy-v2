# Produção: o pay é servido sob o prefixo /pay em api.koopfy.com
# (o nginx do Elastic Beanstalk remove o /pay antes de repassar ao backend).
# NEXT_PUBLIC_* é embutido no bundle em tempo de build (next build).
NEXT_PUBLIC_API_URL=https://api.koopfy.com/pay

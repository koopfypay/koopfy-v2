# Dev: bate direto no backend pay em localhost (sem nginx, então SEM o prefixo /pay).
# O Next carrega este arquivo apenas em `next dev` (NODE_ENV=development).
# Para apontar p/ outra porta/host, crie um .env.local (ignorado pelo git).
NEXT_PUBLIC_API_URL=http://localhost:3007
